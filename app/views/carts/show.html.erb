<section class="cart-show">

  <header class="page-header">
    <h1>My Cart </h1>
  </header>

  <% if (enhanced_cart.length() > 0) %>
    <%= render 'cart_items' %>
    <%= render 'checkout_button' %>
  <% else %>
    <div class="alert alert-info" role="alert">
      Your cart is empty—check out our products <a href="/">here</a>!
    </div>
  <% end %>  
</section>